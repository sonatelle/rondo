# Load the flake development shell when entering this directory.
# Requires direnv; nix-direnv is recommended for caching.
#
# When using languages.rust.version = "file", reload when the
# toolchain file changes (nix-direnv does not watch it by default).
watch_file rust-toolchain.toml
watch_file rust-toolchain
use flake
